# Virtual Platform install steps
1. Install all needed tool by following this [tutorial](TOOLS.md);
2. Install Docker runtime and download the container base image by following this [tutorial](DOCKER.md);
3. Launch the VP by following this [tutorial](LAUNCH.md).